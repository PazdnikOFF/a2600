#pragma once

// Тонкий C++-фасад над libhal.so (референсное железо переиспользуется как есть).
//
// libhal экспортирует 749 функций Hal_* (C-API, extern "C"): питание, источник
// света (Dccu), зонды (Pim), ЭКГ, ЭМ-навигация (ATC3DG), FPGA-по-USB, принтер и т.д.
// Здесь объявляем только то, что нужно текущему этапу; остальное добавляется по мере.
//
// Заголовков libhal у нас нет — сигнатуры восстанавливаются из символов бинарника
// и уточняются по поведению. Все объявления держим в extern "C".
//
// При сборке без libhal (десктоп) HAVE_HAL не определён — фасад возвращает заглушки.

#include <string>

namespace hal {

// Инициализация HAL. На устройстве обязательна до вызова прочих Hal_*.
bool init();

// Пример группы «источник света» (Dccu) — уточнить сигнатуры при интеграции.
// bool setLightIntensity(int percent);
// int  lightIntensity();

// Пример «FPGA по USB» — конфиг/поток видео (Hal_UsbFpga*, 58 функций).
// Наполняется на этапе, когда/если понадобится собственный капчур-путь.

} // namespace hal


#ifdef HAVE_HAL
// --- Реальные объявления из libhal (extern "C"). Дополнять по мере интеграции. ---
extern "C" {
    int Hal_Init_Hal(void);
    // int Hal_Get_...(...);
    // int Hal_Set_...(...);
}
#endif
